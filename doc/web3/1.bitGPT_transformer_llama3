你现在是一名大模型专家，现在需要构建一个生成式模型用于预测股票走势
你将使用python语言，结合Hugging face transformers 中llama3的模型结构，构建一个生成式模型BitGPT用于预测股票走势
输入的tensor维度依次为batch_size, seq_length, feature_size，
其中seq_length为输入的长度为120，用前120分钟的数据预测第121分钟的数据，feature_size为输入的特征维度为5
依次是开盘价，最高价，最低价，收盘价，成交量
源数据在bitstampUSD_1-min_data_2012-01-01_to_2020-12-31.csv中，读取其中的Open, High, Low, Close, Volume_(BTC)字段
数据预处理先对输入的价格和成交量分别进行最大值最小值缩放，其中最大值和最小值是超参数，外部指定的
自定义Embedding层，将输入的tensor映射到dim_size的维度4096维
然后经过32层的TransformerBlock，这个直接复用llama3的模型结构，并冻结参数，不参与训练
输出层再将4096维降到5维，然后对5个维度都计算损失。
请计算出整个模型的所有参数量
请给出完整的数据预处理，自回归模型训练的训练过程，模型的推理部分，推理完成的结果再缩放回去。
请放到一个文件中输出。


你现在是一名大模型专家，现在需要构建一个生成式模型用于预测股票走势
你将使用python语言，结合Hugging face transformers 中llama3的模型结构，构建一个生成式模型BitGPT用于预测股票走势
输入的tensor维度依次为batch_size, seq_length, feature_size，
其中seq_length为输入的长度为120，用前120分钟的数据预测第121分钟的数据，feature_size为输入的特征维度为5
依次是开盘价，最高价，最低价，收盘价，成交量
源数据在bitstampUSD_1-min_data_2012-01-01_to_2020-12-31.csv中，读取其中的Open, High, Low, Close, Volume_(BTC)字段
数据预处理先对输入的价格和成交量分别进行最大值最小值缩放，其中最大值和最小值是超参数，外部指定的
自定义Embedding层，将输入的tensor映射到dim_size的维度4096维
然后经过32层的TransformerBlock，这个直接复用llama3的模型结构，并冻结参数，做LoRA微调
输出层再将4096维降到5维，然后对5个维度都计算损失。
请计算出整个模型的所有参数量
请给出完整的数据预处理，自回归模型训练的训练过程，模型的推理部分，推理完成的结果再缩放回去。
请放到一个文件中输出。

